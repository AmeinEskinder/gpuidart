# Windows acceptance at bb6a894

All nine checks in acceptance.json passed on 2026-09-26. package.json identifies
the candidate, DLLs and source. The named stdout/stderr logs belong to this run;
interaction.json, stability.json, reload.json and package-failures.json were
copied from the SDK verifier outputs after the run.

See ../../README.md for artifact downloads and external gates. Prior root MVP
records are preserved in ../prior-mvp/. Their earlier environment/setup records
do not describe this run.
